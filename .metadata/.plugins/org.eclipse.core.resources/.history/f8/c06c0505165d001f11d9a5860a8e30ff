package actions;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;

import com.utils.neotech.CommonMethods;

public class DragDemo extends CommonMethods {

	public static void main(String[] args) {
		/*
		 * Homework 1: Open chrome browser Go to
		 * https://demo.guru99.com/test/drag_drop.html Drag the following blocks in
		 * different ways Drag BANK to DEBIT SIDE Drag SALES to CREDIT SIDE Drag 5000 to
		 * DEBIT SIDE Drag 5000 to CREDIT SIDE Close the browser
		 * 
		 */
         setUp();
         //DEBIT SIDE
         WebElement bank=driver.findElement(By.id("credit2"));
         WebElement debitAccountArea=driver.findElement(By.xpath("//ol[@id='bank']"));
         WebElement fiveThousand=driver.findElement(By.id("fourth"));
         Actions action=new Actions(driver);
         action.dragAndDrop(bank, debitAccountArea).perform();
         action.dragAndDrop(debitAccountArea, fiveThousand).perform();
         //CREDIT SIDE
         WebElement sales=driver.findElement(By.id("credit1"));
         WebElement creditAccountArea=driver.findElement(By.xpath("//ol[@id='loan']"));
         action.dragAndDrop(sales, creditAccountArea).perform();
         action.dragAndDrop(fiveThousand, creditAccountArea).perform();
         
		
		
	}

}
